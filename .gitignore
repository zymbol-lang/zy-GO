# Editor and OS noise
.vscode/
.idea/
*.swp
.DS_Store

# Zymbol standalone build output
*.out

# Game records produced by 棋戦.zy. A 200-game run is about 630 KB,
# so this is ignored by size convention rather than necessity — see
# 棋戦.md on where a growing dataset should actually live, and set
# ZYGO_KIFU to put it somewhere durable.
棋譜/
