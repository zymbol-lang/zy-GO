# Editor and OS noise
.vscode/
.idea/
*.swp
.DS_Store

# Zymbol standalone build output
*.out

# Game records produced by 棋戦.zy — one directory per run, so a bad
# run is one `rm -rf` away. They live in their own repository:
#   https://github.com/zymbol-lang/zy-GO-kifu
# Set ZYGO_KIFU to write straight into a clone of it.
games/
