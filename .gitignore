# Editor and OS noise
.vscode/
.idea/
*.swp
.DS_Store

# Zymbol standalone build output
*.out

# Game records produced by 棋戦.zy — one directory per run, so a bad
games/

# ignore the build output of the Zymbol compiler, which is a single file
.gitignore